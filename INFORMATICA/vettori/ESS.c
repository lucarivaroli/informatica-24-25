/*dopo avere caricato un vettore numerico confrontare 
la somma delle componenti di indice pari con la somma
delle componenti dispari e dire se la somma maggiore 
e divisibile per un valore assegnato k (diverso da 0)*/

#include<stdio.h>
#include "libreria.h"
#include "libreria.c"
#define dim 4

int main(){
    int vett1[dim];
    int vett2[dim];

    riempiVettore(vett1, dim);
    riempiVettore(vett2, dim);

    do
    {
        printf("inserisci il valore di k");
        scan
    } while (k<0);
    
}